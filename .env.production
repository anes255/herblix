# Empty on purpose: the SPA calls same-origin "/api/*", and Vercel rewrites
# those to the Render backend (see vercel.json). This keeps the login cookie
# first-party so admin auth works in every browser, and avoids CORS entirely.
VITE_API_BASE=
